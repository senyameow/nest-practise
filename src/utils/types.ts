// and here we can define our custom types
// for example we'll need to get username and email to create user
// so we can export type

export type createUserType = {
    username: string,
    email: string,
}